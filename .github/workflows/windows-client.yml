name: Windows client MSI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Build all-in-one MSI
        shell: pwsh
        run: ./windows/publish.ps1
      - name: Verify executable package
        shell: pwsh
        run: |
          if (-not (Test-Path ./windows/dist/win-x64/GOne.Client-x64.msi)) { throw "GOne.Client-x64.msi was not produced" }
      - uses: actions/upload-artifact@v4
        with:
          name: GOne.Client-MSI-win-x64
          path: windows/dist/win-x64/GOne.Client-x64.msi
          if-no-files-found: error
