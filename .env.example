# Copy to .env or run: python3 scripts/ensure_compose_env.py
G_ONE_JWT_SECRET=replace-with-at-least-32-random-bytes
G_ONE_JWT_ISSUER=g-one
G_ONE_JWT_AUDIENCE=g-one-api
# Use 127.0.0.1 instead when an HTTPS reverse proxy runs on this host.
G_ONE_BIND_ADDRESS=0.0.0.0
G_ONE_HTTP_PORT=8000
